# 引き継ぎ書 — AIニュース自動配信システム（2026-06-30 作成）

このチャットで行った作業のまとめ。新しいチャットを始めるときは、これを貼れば続きから進められます。
（恒久メモリにも記録済み：`ai-news-system` / `work-style-autonomy` / `project-folder-moves`）

---

## 1. このセッションでやったこと（時系列）

1. **配信が止まった原因を特定・修正**（2回）
   - 6/28: アプリのフォルダがデスクトップ直下→`claude code`フォルダ内へ移動 → 定期タスク/launch.jsonのパスがズレて失敗。両方修正。
   - 重複フォルダ（OneDriveが復元した）を削除し、`claude code\ai-news-viewer` の1つに統一。
2. **6/28・6/29・6/30 の配信を手動キャッチアップ**（news.json更新＋Drive保存＋push）。
3. **ビューアUIを改修**：縦スクロール積み上げ → 「日付ドロップダウンで選んだ1日だけ表示・既定は今日」。検索/お気に入り時は全日横断。
4. **権限設定を緩和**：`~/.claude/settings.json` に Read/Edit/Write/Bash/WebSearch/WebFetch＋MCP(プレビュー/ドライブ/scheduled-tasks)を全許可。作業中の確認プロンプトを削減。
5. **【本命】毎日の配信を GitHub Actions（クラウド）へ移行**。PC・claude.ai 不要で毎朝7:03 JSTに自動実行。テスト実行成功・本番稼働中。
6. **旧ローカル定期タスク `daily-ai-news` を停止**（enabled:false。二重配信防止）。
7. **月次まとめタスクを news.json 読み込み方式へ変更**（旧：Driveの日次.md読み込み）。

---

## 2. 現在のシステム構成（重要）

### 毎日の配信 ＝ GitHub Actions（クラウド・PC不要）
- リポジトリ: **github.com/shun884i21/ai-news-viewer**（public）
- ワークフロー: `.github/workflows/daily-ai-news.yml`（cron `3 22 * * *` = 毎朝 **7:03 JST**、手動実行ボタンあり）
- スクリプト: `scripts/fetch_daily_news.py`
  - 5媒体のRSS収集（The Verge / TechCrunch / VentureBeat / MIT Tech Review / Bloomberg）
    - VentureBeatは `venturebeat.com/feed/`、BloombergはGoogleニュースRSS経由（site:bloomberg.com）
  - **Claude API（モデル `claude-sonnet-4-6`）** で5本選定＋日本語要約（180〜280字）＋展望をJSON生成
  - news.json の先頭に当日分を追記 → commit/push（同日があれば置換、sample削除）
- 必要なSecret: **`ANTHROPIC_API_KEY`**（登録済み）
  - 課金: Anthropic API 従量（claude.ai月額とは別）。**月350円程度の見込み**。コンソールにクレジット投入済み（残$10.99）。
  - コスト下げたいなら workflow の `NEWS_MODEL` を `claude-haiku-4-5` に変更（月100円台）。

### ビューア（PWA）
- 公開URL: **https://shun884i21.github.io/ai-news-viewer/**
- ローカルクローン: `C:\Users\shuns\OneDrive\デスクトップ\claude code\ai-news-viewer`
- 構成: index.html / app.js / news.json / manifest.json / sw.js / icon.svg（フレームワーク不要・GitHub Pages）
- データ: 単一 news.json に全日分。`{updatedAt, days:[{date, items:[{title,summary,url,source,publishedAt}], outlook, note?}]}`、新しい日が先頭。
- 表示: 日付ドロップダウンで1日ずつ表示（既定=最新日）。検索・お気に入りは全日横断。
- プレビュー: `.claude/launch.json` の "ai-news-viewer"（python http.server, port 4180、`--directory "claude code/ai-news-viewer"`）

### 月次まとめ ＝ ローカル定期タスク（claude.aiアプリ起動時に実行）
- タスク: `monthly-ai-news-digest`（毎月1日 朝8時すぎ、enabled）
- SKILL.md: `C:\Users\shuns\.claude\scheduled-tasks\monthly-ai-news-digest\SKILL.md`
- 動作: **news.json（raw GitHub）から前月分（`date`が`YYYY-MM-`で始まる）を抽出** → 統合してNotebookLM用Googleドキュメントを Driveの「AIニュース」フォルダに作成。
- 次回 7/1 朝に6月分（6/27〜6/30の4日分）が作られる想定。

### 停止中
- `daily-ai-news`（ローカル定期タスク）= **enabled:false**。GitHub Actions版に置き換わったため。再有効化しないこと（二重配信になる）。

---

## 3. 重要なパス・ID・URL

| 項目 | 値 |
|---|---|
| ビューア公開URL | https://shun884i21.github.io/ai-news-viewer/ |
| GitHubリポジトリ | github.com/shun884i21/ai-news-viewer |
| news.json (raw) | https://raw.githubusercontent.com/shun884i21/ai-news-viewer/main/news.json |
| ローカルクローン | `C:\Users\shuns\OneDrive\デスクトップ\claude code\ai-news-viewer` |
| Drive「AIニュース」フォルダID | `1PFzF8Lv8nzdCRtzX8_ajjlVOcxI_thBZ` |
| Actions Secret | `ANTHROPIC_API_KEY`（登録済み） |
| 使用モデル | `claude-sonnet-4-6`（workflowの`NEWS_MODEL`） |

---

## 4. 残課題・気をつける点

- **7/1の朝に月次まとめが正しく作られたか確認**したい（news.json方式の初回実行）。
- GitHub Actions版は **Drive保存をしない**（日次.mdは作られない）。月次はnews.jsonベースに変えたので問題なし。過去（〜6/30）にDriveへ手動保存した日次.mdは残っているが、今後は増えない。
- **フォルダを移動するときは要注意**：定期タスクのSKILL.md／`デスクトップ\.claude\launch.json`／メモリのパスが連動。移動したらClaudeに「◯◯を△△に移した」と伝えれば追従修正する。
- OneDriveが時々フォルダを複製/復元することがある（デスクトップ直下に`ai-news-viewer`が再出現するなど）。出たら重複を消すだけでOK（データはGitHubにある）。

---

## 5. 新しいチャットでの再開のしかた

新チャットでこのファイルを開く or 内容を貼って、「AIニュースシステムの引き継ぎ。続きをお願い」と言えばOK。
恒久メモリ（MEMORY.md経由）にも要点は載っているので、Claudeは基本状況を把握済みのはず。

ユーザーの希望: **仕様/要件の確認以外は止めずに自走してほしい**（細かい確認で何度も止めない）。
